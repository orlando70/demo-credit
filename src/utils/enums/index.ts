export enum TransactionTypeEnum {
  DEPOSIT = 'deposit',
  WITHDRAWAL = 'withdrawal',
  TRANSFER = 'transfer',
}

export enum TransactionStatusEnum {
  SUCCESS = 'success',
  FAIL = 'fail',
  PENDING = 'pending',
}

export enum TokenFlagEnum {
  AUTH = 'auth',
}
